---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-14.04

suites:
  - name: default
    run_list:
  - name: package
    run_list: 
      - recipe[cookbook-graphicsmagick::package]
    attributes:
  - name: depackage
    run_list:
      - recipe[cookbook-graphicsmagick::depackage]
    attributes:
  - name: source
    run_list:
      - recipe[cookbook-graphicsmagick::source]
    attributes:
